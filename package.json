{
  "name": "hello-world",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest"
  },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "jest": "^25.1.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.2.1",
    "tslint": "^6.1.0",
    "typescript": "^3.8.3",
    "lodash": ">=4.17.19"
  }
}
